# import Dog, DogSubscriptionPayload from './generated/prisma.graphql'

type Query {
	dogs: [Dog!]!
	dog(id: ID!): Dog!
}

type Mutation {
	adoptDog(id: ID!): Dog!
}

type Subscription {
  newDog: DogSubscriptionPayload
}